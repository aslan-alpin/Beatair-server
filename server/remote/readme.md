# Beatair Remote Add-On

1) cd server/remote
2) npm i
3) node start-tunnel.js

You’ll get a public URL like `https://beatair-xxxx.loca.lt` and a 6-digit code.
Keep this process running while the server runs.